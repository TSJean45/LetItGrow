import React from 'react';

const DashboardTitle = ({ children }) => {
  return (
    <h2 className="text-5xl text-left font-title py-5 text-gray-800">{children}</h2>
  );
};

export default DashboardTitle;
